# 260521 Handoff

## 작업 목적
- 핫딜 파싱 정확도 개선(가격/구매링크)
- 상세/메인 UI 정리(CTA, 하단 구조, FAB 위치)
- 운영 배포 반영(hotdeal-omega.vercel.app)

## 오늘 반영된 핵심 변경

### 1) 가격 파싱 강화
- 제목/본문 가격 추론 로직 확장
  - `₩/￦ + 숫자` 인식
  - `##,###` 콤마 숫자(원 미표기) 인식
  - 본문 fallback에서 공백 천단위 보정: `919 000원 -> 919,000원`
- 다중 후보 시 숫자 기반 우선 규칙 유지
  - 콤마/1,000원 이상/큰 값 우선

### 2) 루리웹(ruliweb) 구매 링크 추출 개선
- 상세 본문 하단 `출처: http...` 패턴을 buyLink 우선 소스로 사용
- 출처 패턴 없을 때 기존 외부 href fallback 유지

### 3) 상세 페이지 CTA/UI
- 루딜 포함 상세에서 `원본보기 + 가지고싶다` 2버튼 동작 유지
- 버튼 주변 과한 여백 축소/정리
- 상세 페이지에서는 하단 탭 네비게이션 제거

### 4) 메인 페이지 FAB(글쓰기) 조정
- 하단 네비게이션(웹용)은 최종 제거
- FAB를 우측 하단(안드로이드 네비 영역 위 느낌)으로 재배치
- 색상은 브랜드 가지색(`var(--brand)`)으로 원복
- 폰트/크기 축소
- compact(원형) 상태에서 `+`가 원 정중앙에 오도록 정렬 보정

## 검증 포인트
- 루리웹 `id=104244` 케이스
  - 가격: `919,000원`
  - buyLink: 본문 `출처:` URL로 반영
- 상세 페이지: 하단 탭 없음
- 메인 페이지: FAB 위치/색상/축소 상태 중심 정렬 확인

## 주요 변경 파일
- `api/_lib/deals.js`
- `scripts/update_ruliweb_feed.py`
- `detail.html`
- `index.html`
- (중간 생성) `gajigaji.html`, `chat.html`, `my-gaji.html` *(현재 웹 하단탭 제거 정책으로 실사용 없음)*

## 최근 관련 커밋(요약)
- `2929561` FAB 위치 하향 + compact `+` 정중앙 보정
- `7f3d005` FAB 색상/폰트 원복 및 센터 정렬 보정
- `48de9db` 웹 하단 네비 제거 + FAB 위치/스타일 조정
- `edb0086` 메인 전용 하단탭/빈 페이지 생성(이후 정책 변경으로 네비 제거)
- `0c6e651` 루딜 사러가기 복원 + 출처 URL 우선 파싱
- `e17336d` 본문 가격 공백 천단위 파싱 보정

## 배포/브랜치 상태
- 브랜치: `main`
- 원격: `origin` 반영됨
- 운영 도메인: `https://hotdeal-omega.vercel.app`

## 참고
- 저장 파일: `260521handoff.md`
- 민감정보(PAT/Supabase 키)는 문서에 포함하지 않음
